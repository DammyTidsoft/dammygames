version: '3'

services:
  web:
    build: .
    command: sh -c "pip install -r requirements.txt && python manage.py migrate && gunicorn config.wsgi:application --bind 0.0.0.0:8800"
    volumes:
      - .:/tajnid
    ports:
      - "8800:8800"
    restart: unless-stopped